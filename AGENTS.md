# AGENTS.md — 网搜开发说明

## 项目目标

网搜是一个中文动漫发现与播放来源导航。AniList 提供作品资料及其标注的正版入口；用户指定的樱花动漫作为独立第三方页面提供搜索与播放。不得将未经核验的第三方页面标注为正版，也不得抓取、解析、代理或存储视频流，或提供下载、网盘搬运和付费绕过功能。

## 技术结构

- React 19 + TypeScript + Vite 8 + Tailwind CSS 4
- 单页静态站点，无后端、账号或数据库；作品资料来自 AniList API
- `src/App.tsx` 包含搜索、热门榜、播放详情和第三方来源入口
- `src/styles/globals.css` 包含完整视觉系统与响应式规则
- `public/hero-anime-night.png` 为原创主视觉
- `dist/` 为构建输出

## 开发规则

- 正文使用中文，来源名称保留官方写法。
- 新增来源前核验实际域名、搜索和播放路径；无法核实授权、免费或地区限制时不得作出确定性宣称。
- 保持搜索、热门榜、键盘操作、移动端和减少动态效果支持。
- 修改页面后必须执行 `pnpm build`，并在真实浏览器中验证搜索、播放来源切换、桌面端和移动端。
- 发布前保留 Apache-2.0 `LICENSE` 与 `NOTICE` 中的 Aeri 署名。

## 发布

站点由 `.openai/hosting.json` 标识并通过 Sites 发布。GitHub 仓库为 `yue0110-max/wangsou`，用户已明确要求公开创建与推送。
